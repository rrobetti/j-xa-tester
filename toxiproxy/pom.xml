<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>io.github.rrobetti</groupId>
    <artifactId>j-xa-tester</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>xa-tester-toxiproxy</artifactId>
  <name>J XA Tester - Toxiproxy Controller</name>
  <description>
    A minimal HTTP client and fluent controller for the Toxiproxy control
    API (create/enable/disable proxies, add/remove toxics), plus glue that
    turns proxy controls into core XaAction callbacks. Main code depends
    only on the JDK (java.net.http) and xa-tester-core; tests fake the
    Toxiproxy REST API with the JDK's built-in HttpServer instead of a real
    Toxiproxy process or container.
  </description>

  <dependencies>
    <dependency>
      <groupId>io.github.rrobetti</groupId>
      <artifactId>xa-tester-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
