<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>io.github.rrobetti</groupId>
    <artifactId>j-xa-tester</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>xa-tester-jms</artifactId>
  <name>J XA Tester - Jakarta JMS Adapter</name>
  <description>
    Wraps Jakarta Messaging XAConnectionFactory/XAConnection/XASession chains
    with java.lang.reflect.Proxy dynamic proxies so the XAResource they hand
    out is fault-injecting. Main code has no compile-time dependency on the
    Jakarta Messaging API (or any provider); jakarta.jms-api is a test-only
    dependency used to write realistic adapter tests.
  </description>

  <dependencies>
    <dependency>
      <groupId>io.github.rrobetti</groupId>
      <artifactId>j-api-proxy-jms-jakarta</artifactId>
    </dependency>
    <dependency>
      <groupId>io.github.rrobetti</groupId>
      <artifactId>xa-tester-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>jakarta.jms</groupId>
      <artifactId>jakarta.jms-api</artifactId>
    </dependency>
  </dependencies>
</project>
